<%= form_for(cliente, html: {class: ""}) do |f| %>
  <% if cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table class = "table borderless">
    <tr>
      <td class="text-success td_text_right" style="padding-top:4%;">Nombre:</td>
      <td style="text-align:left">
        <%= f.text_field :nom_cliente, :class => 'form-control' %>
      </td>
    </tr>
    <tr>
      <td class="text-success td_text_right" style="padding-top:4%;">Apellido Paterno:</td>
      <td style="text-align:left">
        <%= f.text_field :ap_paterno, :class => 'form-control' %>
      </td>
    </tr>
    <tr>
      <td class="text-success td_text_right" style="padding-top:4%;">Apellido Materno:</td>
      <td style="text-align:left">
        <%= f.text_field :ap_materno, :class => 'form-control' %>
      </td>
    </tr>
    <tr>
      <td class = "text-success td_text_right" style="padding-top:4%;">RFC:</td>
      <td style="text-align:left">
        <%= f.text_field :rfc, :class => 'form-control' %>
      </td>
    </tr>
  </table>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>